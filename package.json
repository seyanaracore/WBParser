{
  "name": "wb-parser",
  "dependencies": {},
  "scripts": {
    "build": "lerna run build --scope=frontend",
    "serve": "lerna run serve --scope=frontend",
    "server": "lerna run server --scope=server"
  },
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
